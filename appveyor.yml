version: 0.1.{build}
environment:
  matrix:
    - PYTH: C:\\Python36\\python.exe
      PYINSTALLER: C:\\Python36\\Scripts\\pyinstaller.exe
      PYTHON_ARCH: "32"
    
    - PYTH: C:\\Python36-x64\\python.exe
      PYINSTALLER: C:\\Python36-x64\\Scripts\\pyinstaller.exe
      PYTHON_ARCH: "64"

init:
  - ECHO %PYTHON_ARCH%
  - %PYTH% --version
install:
  - %PYTH% -m pip install -U pip 
  - %PYTH% -m pip install pyinstaller==3.3
  - %PYTH% -m pip install -r requirements.txt
build_script:
  - call scripts/winbuild.bat
artifacts:
  - path: dist/*
